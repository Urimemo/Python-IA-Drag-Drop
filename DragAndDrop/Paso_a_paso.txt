python 3.8.0 (64 bits)
python -m pip install --upgrade pip
pip install opencv-contrib-python
pip install mediapipe
pip install autopy
pip install cvzone
pip install msvc-runtime

//importante: rezar para que no de error



Librerias totales:
collected packages: flatbuffers, six, pyparsing, protobuf, pillow, packaging, kiwisolver,
 fonttools, cycler, contourpy, attrs, absl-py, python-dateutil, matplotlib, mediapipe
